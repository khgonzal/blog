import React from 'react';
import { StyledFooter, StyledLink } from './styles.d';

export const Footer = () => {
  return (
    <StyledFooter>
      <StyledLink href="mailto:thecreativedev0@gmail.com">this is the footer</StyledLink>
    </StyledFooter>
  );
};
